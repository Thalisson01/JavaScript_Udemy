const a = 7
let b = 20

// Soma
b += a // b = b + a
console.log(b)

// Subtracao
b -= a // b = b - a
console.log(b)

// Multiplicacao
b *= a // b = b * a
console.log(b)

// Divi
b /= a // b = b / a
console.log(b)

// Resto
b %= a // b = b % a
console.log(b)